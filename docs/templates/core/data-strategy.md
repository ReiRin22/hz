# データ戦略

> 最終更新: YYYY-MM-DD

## データベース選定

| 用途 | DB | 理由 |
|------|-----|------|
| メインDB | | |
| キャッシュ | | |
| 検索 | | |

## モデリング規約

### 命名規則

| 対象 | 規則 | 例 |
|------|------|-----|
| テーブル名 | {{snake_case, 複数形}} | `user_profiles` |
| カラム名 | {{snake_case}} | `created_at` |
| 外部キー | {{参照先_id}} | `user_id` |
| インデックス | {{idx_テーブル_カラム}} | `idx_users_email` |

### 共通カラム

全テーブルに以下を含める:

| カラム | 型 | 用途 |
|--------|-----|------|
| `id` | {{UUID / SERIAL}} | 主キー |
| `created_at` | timestamp | 作成日時 |
| `updated_at` | timestamp | 更新日時 |

## マイグレーション方針

- ツール: {{prisma migrate / knex / typeorm}}
- 命名: `YYYYMMDD_HHMMSS_description`
- 破壊的変更: {{手順を記載。ダウンタイム許容？段階的移行？}}
- ロールバック: {{各マイグレーションにdown必須？}}

## 共有モデル

複数機能が参照するモデルはここに列挙する:

| モデル | 定義場所 | 参照する機能 |
|--------|---------|------------|
| | | |

## バックアップ・リカバリ

- バックアップ頻度: {{日次等}}
- 保持期間: {{30日等}}
- リカバリ手順: {{概要}}
