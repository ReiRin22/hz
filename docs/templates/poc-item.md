# PoC項目 #{N}: {項目名}

## 1. 検証の目的

{なぜこの項目を検証するか・リスクと検証の焦点}

## 2. 実装計画

### 実装内容
- ファイル構成: `poc/{target_en}/{N}_{略称}/` 配下のファイルと役割（`{target_en}` はターゲット名の英語表記）
- 言語・ライブラリ: {言語} / {使用ライブラリ・バージョン}

### 前提環境
- 必要なDB・ツール・バージョン
- 起動コマンド（例: `docker compose up -d`）

### 合否判定ロジック
| 合否基準（PoC開発計画より） | 閾値 | 判定方法 |
|------------------------|------|---------|
| {基準名} | {数値} | {どの出力値と比較するか} |

### 工数見積もり
- 実装: 約X時間
- 実行・確認: 約X時間

---

## 3. 実装詳細

（Step3-B 完了後に AI が追記）

### 実装した主要な技術要素
{設計上のポイント・工夫・制約への対応}

### コード抜粋
{検証の核となる部分のコード}

---

## 4. 実行結果

（Step3-C: 人間が実行ログ・測定値を貼り付け）

### 定性評価（人間）

（Step3-C: 実行時の所見・懸念事項・予期せぬ挙動を記入）

---

## 5. 合否判定

### AI評価（定量） — poc-evaluator

（Step3-C 後に poc-evaluator が記入）

| 合否基準 | 閾値 | 実測値 | 結果 |
|---------|------|--------|------|
| {基準名} | {閾値} | （実行結果より） | ○/× |

### 総合判定 — メインエージェント

（Step3-D: AI評価 + 人間評価を統合して記入）

**判定: 合格 / 不合格 / 条件付き合格 / スキップ**

**判定理由**: {根拠の説明}

**本実装への引き継ぎ事項**: {採用時の注意点・制約}
