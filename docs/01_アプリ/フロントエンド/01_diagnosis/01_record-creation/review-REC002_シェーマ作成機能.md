# レビュー結果_【REC002】シェーマ作成機能

- **対象**: design_detail-REC002_シェーマ作成機能.md
- **実施日**: 2026-04-20
- **判定**: FAIL

---

## High（5件）

| # | 対象セクション | 指摘内容 |
|---|--------------|---------|
| H1 | AI実装制約 / EVT_INIT_SCHEMA | **初期表示が「監査ログ対象」になっているが意図が無記載。** 読み取り操作の監査を意図するなら理由を備考に記載。意図しないなら「非対象」に修正すること |
| H2 | 操作イベント定義 | **画面起動時の初期テンプレート一覧取得フローが未定義。** `GET /bff/templates` がAPI一覧にあるが、起動時（初期値「全身」）のフロー記述が操作イベント定義に存在しない |
| H3 | 状態管理 / `hasDrawContent` | **`hasDrawContent = true` の判定条件が未定義。** キャンセル確認ダイアログ（E003）と確定時エラー（E002）の発火条件に直結。「背景テンプレートのみ設定」「テキスト入力後に全削除」等が true/false どちらになるか不明 |
| H4 | 操作イベント定義 / テンプレートパネル | **お気に入り登録・解除のイベント定義・永続化仕様が完全欠落。** EVT_*/ACT_* テーブルに☆ボタン操作が一件も存在しない。APIも呼び出し一覧に未登録（ローカル保存なら旨を明記） |
| H5 | 画面設計 / No.2 描画エリア | **「有効テンプレートのみ選択可能」の「有効」判定基準が未定義。** `GET /bff/templates` レスポンスのどのフラグで判断するかが不明 |

---

## Medium（7件）

| # | 対象セクション | 指摘内容 |
|---|--------------|---------|
| M1 | UI制御 / テンプレート選択 | テンプレート切替・クリア時の確認ダイアログ有無が未定義（キャンセルはあり、他はなし—という意図であれば明示が必要） |
| M2 | EVT_IMG_PASTE | Ctrl+V でjpg/png以外（WebP・テキスト等）を貼り付けた場合の挙動が未定義。E001 がペースト時にも発火するか不明 |
| M3 | 画面設計 / テキストツール | 入力確定タイミング（Enter/ツール切替/Canvas外クリック等）・フォーカス制御・Undoの対象単位が未定義 |
| M4 | 状態管理 / Undo/Redo | スタック上限・超過時の挙動（最古破棄等）・`EVT_CLEAR` 自体のUndoの可否が未定義 |
| M5 | EVT_FLIP | 背景テンプレート画像も反転対象に含まれるか否かが未定義。部位の左右逆転は医療上重要 |
| M6 | 呼び出しAPI一覧 | 描画データの保存形式（Base64 PNG / SVG / 独自オブジェクト等）が本設計書に存在せず「BFF定義書参照」に委ねられている。フロントエンドの送出形式を本書に明記すること |
| M7 | 呼び出しAPI一覧 | お気に入り登録・解除 API が未登録（H4 と連動） |

---

## Low（4件）

- `activeTool` の型が `string` で取りうる値の列挙（Union型）が未定義
- 全ロールの操作権限が同一か否かの明示がない（「三者とも全操作が可能」と明記すべき）
- ブラウザを閉じた場合（`beforeunload`）の扱いが未定義
- 行数 278行 → 300行制限内（問題なし）

---

## 整合性チェック

| チェック | 結果 |
|---------|------|
| CLAUDE.md ↔ INDEX.md（F01） | OK |
| REC002 が INDEX.md に未登録 | 要対応 |
| 設計書ファイルの存在 | OK |

REC002 の設計書は存在するが INDEX.md にエントリがありません。またファイルが `docs/01_アプリ/フロントエンド/` 直下に置かれており、ルール2「1機能 = 1フォルダ（`{domain}/機能グループ名/`配下）」との整合も確認が必要です。

---

## 次のアクション

FAIL のため次フェーズへの遷移は保留。High 5件を解消してから再レビューが必要。
