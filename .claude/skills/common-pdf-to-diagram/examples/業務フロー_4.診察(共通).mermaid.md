# 診察（共通）業務フロー - マーメイド図

```mermaid
flowchart TD
    %% スタイル定義
    classDef startNode fill:#90EE90,stroke:#2d7600,stroke-width:2px
    classDef processNode fill:#87CEEB,stroke:#1971c2,stroke-width:2px
    classDef decisionNode fill:#FFD700,stroke:#d6b656,stroke-width:2px
    classDef dataNode fill:#F0E68C,stroke:#c6c189,stroke-width:2px
    classDef endNode fill:#FFB6C1,stroke:#d6657b,stroke-width:2px
    classDef systemNode fill:#E8E8E8,stroke:#666,stroke-width:2px

    %% ノード定義
    START(["診察室入室"]):::startNode
    
    subgraph NURSE["看護師等"]
        STEP1["1.患者呼び込み<br/>本人確認"]:::processNode
        STEP7["7.皮膚状態等の<br/>写真撮影<br/>画像取り込み"]:::processNode
    end
    
    subgraph DOCTOR["医師"]
        STEP2["2.問診票、過去診療記録<br/>患部の確認"]:::processNode
        STEP3["3.オンライン資格確認等<br/>システムの<br/>医療情報・受診歴等の閲覧"]:::processNode
        STEP4["4.電子処方箋管理<br/>サービスの<br/>調剤結果の閲覧"]:::processNode
        STEP5["5.電子カルテ情報共有<br/>サービスの<br/>診療情報提供書の閲覧"]:::processNode
        STEP6["6.患者プロファイル<br/>の参照"]:::processNode
        STEP8["8.医療用画像の閲覧"]:::processNode
        STEP9["9.医療面接<br/>(主訴等確認)、触診"]:::processNode
        DECISION1{"検査結果<br/>有り？"}:::decisionNode
        STEP10["10.診断内容や治療<br/>計画・処方内容の説明<br/>療養上のアドバイス・指導"]:::processNode
        STEP11["11.診療記録(SOAP)<br/>の登録"]:::processNode
        STEP12["12.傷病名の登録"]:::processNode
    end
    
    subgraph CLERK["事務職員"]
        STEP13["13.指導・管理料、<br/>摘要欄コメント等の<br/>コスト情報登録"]:::processNode
        STEP14["14.外来記録の登録<br/>(診療情報提供書等)"]:::processNode
        DECISION2{"次回予約または<br/>他院紹介<br/>必要あり？"}:::decisionNode
    end
    
    subgraph EHR["電子カルテ"]
        DATA_PROFILE[("患者プロファイル")]:::dataNode
        DATA_SOAP[("診療記録<br/>(SOAP)")]:::dataNode
        DATA_DISEASE[("傷病名")]:::dataNode
        DATA_DOCUMENT[("文書管理")]:::dataNode
    end
    
    subgraph MEDICAL_DX["医療DX"]
        SYS_QUALIFICATION[("オンライン資格<br/>確認等システム")]:::systemNode
        SYS_PRESCRIPTION[("電子処方箋<br/>管理サービス")]:::systemNode
        SYS_EHR_SHARING[("電子カルテ情報<br/>共有サービス")]:::systemNode
    end
    
    subgraph OTHER_SYSTEM["他システム"]
        DEVICE_CAMERA[("スキャナ・デジカメ")]:::systemNode
        SYS_PACS[("PACS<br/>(画像システム)")]:::systemNode
        SYS_MEDICAL_ACCOUNTING[("医事システム")]:::systemNode
    end
    
    END(["診察終了"]):::endNode
    
    %% フロー接続
    START --> STEP1
    STEP1 --> STEP2
    STEP2 --> STEP3
    STEP3 -.-> SYS_QUALIFICATION
    STEP3 --> STEP4
    STEP4 -.-> SYS_PRESCRIPTION
    STEP4 --> STEP5
    STEP5 -.-> SYS_EHR_SHARING
    STEP5 --> STEP6
    STEP6 -.-> DATA_PROFILE
    STEP6 --> STEP7
    STEP7 -.-> DEVICE_CAMERA
    STEP7 --> STEP8
    STEP8 -.-> SYS_PACS
    STEP8 --> STEP9
    STEP9 --> DECISION1
    
    DECISION1 -->|"無し"| STEP10
    DECISION1 -->|"有り"| STEP9
    
    STEP10 --> STEP11
    STEP11 -.-> DATA_SOAP
    STEP11 --> STEP12
    STEP12 -.-> DATA_DISEASE
    STEP12 --> STEP13
    STEP13 --> STEP14
    STEP13 -.-> SYS_MEDICAL_ACCOUNTING
    STEP14 -.-> DATA_DOCUMENT
    STEP14 --> DECISION2
    
    DECISION2 -->|"いいえ"| END
    DECISION2 -->|"はい"| STEP1
```

## 凡例

### ノードの種類
- **楕円（緑）**: 開始/終了ノード
- **四角（青）**: 処理ステップ
- **ひし形（黄）**: 判断分岐
- **円柱（黄）**: データストア/データベース
- **円柱（灰）**: 外部システム

### 線の種類
- **実線（→）**: メインフロー
- **点線（-.->）**: データ参照・システム連携

## 主要フロー

1. **患者受付・情報参照**
   - 診察室入室 → 本人確認 → 過去診療記録参照
   - 医療DXサービスからの情報取得（オンライン資格確認、電子処方箋、診療情報提供書）

2. **診察・検査**
   - 患者プロファイル参照 → 画像撮影・閲覧 → 医療面接
   - 検査結果がある場合は再度検査結果確認

3. **記録・登録**
   - 診断内容説明 → SOAP記録登録 → 傷病名登録
   - コスト情報登録 → 外来記録登録

4. **診察完了**
   - 次回予約または他院紹介が必要な場合は再受付へ
   - 不要な場合は診察終了

## システム連携

### 医療DX基盤
- オンライン資格確認等システム
- 電子処方箋管理サービス
- 電子カルテ情報共有サービス

### 院内システム
- PACS（画像システム）
- 医事システム
- スキャナ・デジカメ

### データベース
- 患者プロファイル
- 診療記録（SOAP）
- 傷病名
- 文書管理
